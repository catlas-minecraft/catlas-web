{
  "name": "@catlas/api",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "The server template",
  "repository": {
    "type": "git",
    "url": "<PLACEHOLDER>",
    "directory": "packages/api"
  },
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "scripts": {
    "dev": "DATABASE_URL=postgresql://catlas:catlas@127.0.0.1:5432/catlas JWT_SECRET=dev-secret portless --name api.catlas node --import tsx/esm src/bin/dev.ts",
    "codegen": "build-utils prepare-v2",
    "build": "pnpm build-esm && pnpm build-annotate && pnpm build-cjs && build-utils pack-v2",
    "build-esm": "tsc -b tsconfig.build.json",
    "build-cjs": "babel build/esm --plugins @babel/transform-export-namespace-from --plugins @babel/transform-modules-commonjs --out-dir build/cjs --source-maps",
    "build-annotate": "babel build/esm --plugins annotate-pure-calls --out-dir build/esm --source-maps",
    "check": "tsc -b tsconfig.json",
    "test": "vitest",
    "coverage": "vitest --coverage"
  },
  "dependencies": {
    "@catlas/db": "workspace:^",
    "@catlas/domain": "workspace:^",
    "@catlas/louis": "workspace:^",
    "@effect/experimental": "^0.60.0",
    "@effect/platform": "0.96.0",
    "@effect/platform-node": "0.106.0",
    "@effect/sql": "0.51.0",
    "@effect/sql-kysely": "0.47.0",
    "effect": "3.21.0",
    "kysely": "^0.28.14",
    "portless": "^0.7.2"
  },
  "effect": {
    "generateExports": {
      "include": [
        "**/*.ts"
      ]
    },
    "generateIndex": {
      "include": [
        "**/*.ts"
      ]
    }
  },
  "devDependencies": {
    "oxfmt": "^0.41.0"
  }
}
